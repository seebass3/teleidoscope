import { Page } from '@/sanity.types'

type OfferingLayoutProps = {
	page: Page
}

export default function OfferingLayout({ page }: OfferingLayoutProps) {
	return <div className="container">Stuff</div>
}
